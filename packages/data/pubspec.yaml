name: data
description: Carlet domain layer for data manipulation.
publish_to: none

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.5
  http: ^1.1.0
  http_status: ^3.2.0
  bloc: ^8.1.4
  flutter_bloc: ^8.1.5
  streaming_shared_preferences: ^2.0.0
  pusher_client: ^2.0.0

dev_dependencies:
    json_serializable: ^6.8.0
    build_runner: ^2.4.9